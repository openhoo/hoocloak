apiVersion: v1
kind: Service
metadata:
  name: {{ include "hoocloak.fullname" . | quote }}
  labels:
    {{- include "hoocloak.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type | quote }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "hoocloak.selectorLabels" . | nindent 4 }}
